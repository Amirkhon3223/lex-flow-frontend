# LexFlow - Юридическая CRM система

> Современное React-приложение для управления юридической практикой с Feature-Sliced Design архитектурой

## 🚀 Быстрый старт

```bash
npm install
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## 📋 О проекте

LexFlow - это полнофункциональная CRM система для юридических компаний, включающая:

- 📊 **Dashboard** - Обзор активных дел, задач и статистики
- 👥 **Клиенты** - Управление базой клиентов (физ. лица, ИП, юр. лица)
- ⚖️ **Дела** - Ведение судебных и несудебных дел
- 📄 **Документы** - Управление документами с версионированием
- 📅 **Календарь** - Планирование встреч и событий
- 📈 **Аналитика** - Детальная статистика и отчёты
- 🤖 **AI Помощник** - Интеллектуальный анализ документов
- ⚙️ **Настройки** - Управление профилем и подпиской

## 🏗️ Архитектура проекта

### Feature-Sliced Design (FSD)

Проект следует принципам FSD архитектуры для максимальной масштабируемости и поддерживаемости.

```
src/
├── app/                    # Конфигурация приложения
│   ├── config/            # API конфиг, роуты
│   ├── guards/            # Auth guards (отключен для разработки)
│   ├── services/          # API сервисы
│   ├── store/             # Zustand stores
│   ├── types/             # TypeScript типы и enums
│   └── router.tsx         # React Router конфигурация
│
├── modules/               # Бизнес-логика и компоненты
│   ├── dashboard/
│   │   ├── index.tsx      # Главная страница модуля
│   │   ├── widgets/       # Виджеты (StatsCards, PriorityCases...)
│   │   └── ui/            # UI компоненты модуля
│   ├── clients/
│   │   ├── index.tsx
│   │   ├── components/    # Дополнительные страницы
│   │   ├── pages/         # ClientDetailPage
│   │   └── ui/            # UI компоненты
│   ├── cases/
│   ├── documents/
│   ├── calendar/
│   ├── analytics[about.md](..%2F..%2FDownloads%2Fabout.md)/
│   ├── ai-assistant/
│   ├── settings/
│   ├── reports/
│   └── notifications/
│
├── pages/                 # Обёртки страниц для роутинга (2 строки)
│   ├── DashboardPage.tsx
│   ├── ClientsPage.tsx
│   └── Index.tsx      # Полноценная страница
│
└── shared/                # Переиспользуемые компоненты
    ├── ui/                # shadcn/ui компоненты
    ├── components/        # Layout, Header, Sidebar
    └── utils/             # Утилиты
```

### Ключевые принципы

#### 1. **modules/** - Вся бизнес-логика
`index.tsx` каждого модуля - это главная страница, собранная из мелких UI компонентов:

```tsx
// modules/dashboard/index.tsx
import { StatsCards } from './widgets/StatsCards';
import { PriorityCases } from './widgets/PriorityCases';

export default function DashboardPage() {
  return (
    <div>
      <StatsCards />
      <PriorityCases />
    </div>
  );
}
```

#### 2. **pages/** - Простые обёртки
Каждый файл в `pages/` - это 2 строки для чистого роутинга:

```tsx
// pages/DashboardPage.tsx
export { default } from '@/modules/dashboard';
```

#### 3. **ui/** в каждом модуле
Мелкие переиспользуемые UI компоненты, специфичные для модуля:

```
modules/clients/ui/
├── AddClientDialog.tsx
├── EditClientDialog.tsx
├── ClientCard.tsx
├── SearchBar.tsx
└── StatsCards.tsx
```

## 📦 Технологический стек

### Core
- **React 19** - UI библиотека
- **TypeScript 5.9** - Типизация
- **Vite** - Сборщик (Rolldown)
- **React Router 7** - Роутинг

### UI
- **Tailwind CSS 4** - Стилизация
- **shadcn/ui** - UI компоненты
- **Radix UI** - Headless компоненты
- **Lucide React** - Иконки
- **Recharts** - Графики и диаграммы

### State Management
- **Zustand** - Глобальное состояние
- **React Hook Form** - Формы
- **Zod** - Валидация схем

### API
- **Axios** - HTTP клиент
- **TanStack Query** - Data fetching

## 🛠️ ESLint конфигурация

### Правила линтера:
- ❌ **Запрещено использование `any`**
- ✅ **Обязательные точки с запятой**
- ✅ **Trailing commas** только где есть продолжение
- ✅ **Автоматическая сортировка импортов** (React → внешние → внутренние)
- ✅ **Удаление неиспользуемых импортов**

### Команды:
```bash
npm run lint          # Проверка ошибок
npm run lint:fix      # Автофикс (включая сортировку импортов)
```

## 📝 Скрипты

```bash
npm run dev           # Запуск dev сервера
npm run build         # Продакшн сборка
npm run preview       # Предпросмотр продакшн сборки
npm run lint          # ESLint проверка
npm run lint:fix      # Автофикс ошибок линтера
```

## 🔐 Конфигурация

Создайте `.env` файл:

```env
VITE_API_BASE_URL=http://localhost:3000/api
```

## 🎨 Особенности UI

### Дизайн-система
- **Цветовая палитра:** Профессиональная синяя гамма
- **Типографика:** Inter font family
- **Компоненты:** Единый стиль на основе shadcn/ui
- **Адаптивность:** Полностью responsive дизайн
- **Анимации:** Плавные transitions

### Ключевые компоненты
- Cards с градиентами
- Badges для статусов
- Avatars с fallback
- Dialogs для форм
- Tabs для навигации
- Progress bars для дел
- Charts для аналитики

## 🗺️ Маршруты

### С Layout (Sidebar + Header):
- `/` - Dashboard
- `/clients` - Клиенты
- `/clients/:id` - Детали клиента
- `/cases` - Дела
- `/cases/:id` - Детали дела
- `/documents` - Документы
- `/documents/:id/versions` - Версии документа
- `/documents/:id/compare` - Сравнение версий
- `/calendar` - Календарь встреч
- `/analytics` - Аналитика и отчёты
- `/ai-assistant` - AI Помощник
- `/reports` - Отчёты
- `/notifications` - Уведомления
- `/settings` - Настройки
- `/settings/profile` - Профиль пользователя

### Без Layout:
- `/login` - Вход в систему

## 🔧 Path Aliases

Настроены алиасы для удобного импорта:

```typescript
import { Button } from '@/shared/ui/button';
import { clientService } from '@/app/services/clients/clientService';
import { ClientCard } from '@/modules/clients/ui/ClientCard';
```

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)
