export enum CaseStatusEnum {
  IN_PROGRESS = 'in_progress',
  REVIEW = 'review',
  COMPLETED = 'completed',
  ON_HOLD = 'on_hold',
}

export enum CasePriorityEnum {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}
